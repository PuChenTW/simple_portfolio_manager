.venv/
__pycache__/
.pytest_cache/
.ruff_cache/
*.py[cod]
*.db
*.db-shm
*.db-wal
.coverage
.playwright-mcp/
CLAUDE.local.md
.temp
.DS_Store

# Frontend build output and dependencies. The built dashboard is a build artifact, produced by
# `bun run build` in frontend/ and baked into the image at Docker build time.
node_modules/
src/portfolio_manager/static/v2/
