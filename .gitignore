.venv/
__pycache__/
.pytest_cache/
.ruff_cache/
*.py[cod]
*.db
*.db-shm
*.db-wal
.coverage
.playwright-mcp/
CLAUDE.local.md
.temp
.DS_Store
.claude
.agents

# Frontend build output and dependencies. The whole static/ directory is a build artifact now,
# produced by `bun run build` in frontend/ and baked into the image at Docker build time. The
# app serves every endpoint without it, so a fresh checkout runs before the frontend is built.
node_modules/
src/portfolio_manager/static/
